/*
================================================
نام فایل:
TransactionRepositoryImpl.kt

وظیفه:
پیاده سازی دسترسی تراکنش‌های مالی.

ارتباط:
Transaction Domain ← Repository ← DAO

نکات توسعه:
منطق ثبت مالی در UseCase قرار می‌گیرد.
================================================
*/

package com.hesabdari.data.repository.impl

class TransactionRepositoryImpl {
    // اتصال TransactionDao در مرحله بعد تکمیل می‌شود.
}
