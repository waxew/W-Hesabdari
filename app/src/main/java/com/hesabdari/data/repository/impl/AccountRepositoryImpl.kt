/*
================================================
نام فایل:
AccountRepositoryImpl.kt

وظیفه:
پیاده سازی Repository حساب‌ها.

ارتباط:
Domain Repository ← این کلاس ← DAO

نکات توسعه:
این فایل نباید شامل منطق کسب و کار باشد.
فقط مسئول اتصال لایه Domain به Data است.
================================================
*/

package com.hesabdari.data.repository.impl

class AccountRepositoryImpl {
    // پیاده سازی اتصال AccountDao در مرحله اتصال Room تکمیل می‌شود.
}
