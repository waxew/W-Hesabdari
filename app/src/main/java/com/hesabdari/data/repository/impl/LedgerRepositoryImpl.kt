/*
================================================
نام فایل:
LedgerRepositoryImpl.kt
وظیفه:
پیاده سازی دسترسی دفتر کل
ارتباط:
Ledger Domain -> Repository -> Data Layer
نکات توسعه:
دفتر کل بخشی از هسته حسابداری عمومی است و مستقل از Business Profile می‌باشد.
================================================
*/

package com.hesabdari.data.repository.impl

class LedgerRepositoryImpl {
    // عملیات دفتر کل در مراحل بعدی تکمیل می‌شود.
}
