/*
================================================
نام فایل:
PaymentRepositoryImpl.kt
وظیفه:
پیاده سازی Repository پرداخت
ارتباط:
Payment Domain -> Repository -> Data Layer
نکات توسعه:
این کلاس فقط مسئول اتصال لایه‌ها است و منطق کسب و کار در آن قرار نمی‌گیرد.
================================================
*/

package com.hesabdari.data.repository.impl

import com.hesabdari.data.repository.PaymentRepository

class PaymentRepositoryImpl : PaymentRepository {
    // پیاده سازی عملیات پرداخت در این بخش تکمیل می‌شود.
}
