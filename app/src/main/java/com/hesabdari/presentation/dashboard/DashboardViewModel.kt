/*
================================================
نام فایل:
DashboardViewModel.kt

وظیفه:
مدیریت وضعیت صفحه داشبورد حسابداری.

ارتباط:
Dashboard UI -> ViewModel -> UseCase

نکات توسعه:
این ViewModel فقط وضعیت نمایش را مدیریت می‌کند.
محاسبات مالی باید در Core و UseCase انجام شود.
================================================
*/

package com.hesabdari.presentation.dashboard

import androidx.lifecycle.ViewModel

class DashboardViewModel : ViewModel() {

    // وضعیت‌های داشبورد در نسخه‌های بعدی از UseCase دریافت می‌شوند.
}
